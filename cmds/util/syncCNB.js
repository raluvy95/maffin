module.exports = {
    name: "sync",
    owner: true,
    run(message, args, client) {
        // this should change nickname to all members in the server
        // but due to unable to cache all members (even with enabled intents)
        // I will leave this as empty untill the issue is resolved
    }
}
